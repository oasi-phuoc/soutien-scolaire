"use client";

/**
 * Ancienne barre d'actions bureau pour les exercices.
 * Désactivée : en leçon on réutilise le style de la barre mobile (MainNav),
 * y compris sur grand écran — la sidebar latérale reste inchangée.
 */
export function ExerciseToolbar() {
  return null;
}
