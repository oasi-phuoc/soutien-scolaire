/** Classes de correction harmonisées — ambre partout (lecture, français, maths). Light mode uniquement. */
export const CORRECTION_BORDER = "border-amber-400";
export const CORRECTION_BG = "bg-amber-50";
export const CORRECTION_TEXT = "text-amber-700";
export const CORRECTION_TEXT_STRONG = "text-amber-600";
export const CORRECTION_CARD = `${CORRECTION_BORDER} ${CORRECTION_BG}`;
export const CORRECTION_BUTTON = `${CORRECTION_BORDER} ${CORRECTION_BG} ${CORRECTION_TEXT}`;
export const CORRECTION_CARD_DARK = CORRECTION_CARD;
export const CORRECTION_INLINE = `${CORRECTION_BORDER} ${CORRECTION_BG} ${CORRECTION_TEXT_STRONG}`;
export const CORRECTION_INLINE_STRIKE = `${CORRECTION_INLINE} line-through`;
export const CORRECTION_MATCH_WRONG = `${CORRECTION_BORDER} ${CORRECTION_BG} ${CORRECTION_TEXT_STRONG}`;
export const CORRECTION_UNDERLINE = "rounded-none border-0 border-b-2 border-amber-500";
export const CORRECTION_ACCENT = "#f59e0b";
