"use client";

/**
 * Ancienne barre « mode édition » (navigation pages élève).
 * Désactivée : édition uniquement sur `/admin/contenu` en bureau.
 */
export function EditModeBar() {
  return null;
}
