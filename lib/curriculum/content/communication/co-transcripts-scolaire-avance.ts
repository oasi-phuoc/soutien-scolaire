/** Transcriptions CO avance scolaire / B1 (saisie manuelle depuis le livre). */
import raw from "./co-transcripts-scolaire-avance.json";

export const CO_TRANSCRIPTS_SCOLAIRE_AVANCE: Record<string, string> = raw as Record<string, string>;
